{
  "name": "stateless-discord-bot",
  "version": "1.0.0",
  "author": "meizuflux",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "miniflare --live-reload --debug --host localhost --port 8000",
    "prediscord": "npx rimraf dist && tsc scripts/commands.ts --outDir dist",
    "discord": "node dist/scripts/commands.js"
  },
  "devDependencies": {
    "miniflare": "^2.2.0",
    "node-fetch": "^3.2.0",
    "typescript": "^4.5.5"
  }
}
